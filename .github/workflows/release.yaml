name: Release Agent
run-name: Release Agent ${{ inputs.tag }}

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing Git tag to release, for example v1.2.3
        required: true
        type: string

permissions:
  contents: read
  packages: write

concurrency:
  group: release-agent-${{ inputs.tag }}
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  REGISTRY: ghcr.io
  CHART_DIR: ./helm/agent
  IMAGE_NAME: resilience-agent

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out tag
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}
          fetch-depth: 1

      - name: Compute metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ inputs.tag }}"

          if [[ ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Tag must look like v1.2.3" >&2
            exit 1
          fi

          version="${tag#v}"
          image_repo="${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"
          chart_repo="oci://${{ env.REGISTRY }}/${{ github.repository_owner }}/charts"

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "image_repo=$image_repo" >> "$GITHUB_OUTPUT"
          echo "chart_repo=$chart_repo" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Log in to GHCR for Docker
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Log in to GHCR for Helm
        shell: bash
        run: |
          set -euo pipefail
          echo "${{ github.token }}" | helm registry login "${{ env.REGISTRY }}" \
            --username "${{ github.actor }}" \
            --password-stdin

      - name: Build and push agent image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/agent.Dockerfile
          target: base
          push: true
          tags: |
            ${{ steps.meta.outputs.image_repo }}:${{ steps.meta.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Lint chart
        shell: bash
        run: |
          set -euo pipefail
          helm lint "$CHART_DIR" \
            --set image.repository="${{ steps.meta.outputs.image_repo }}" \
            --set image.tag="${{ steps.meta.outputs.version }}"

      - name: Package chart
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p dist

          helm package "$CHART_DIR" \
            --version "${{ steps.meta.outputs.version }}" \
            --app-version "${{ steps.meta.outputs.version }}" \
            --destination dist

      - name: Push chart to GHCR
        shell: bash
        run: |
          set -euo pipefail

          chart_package="$(find dist -maxdepth 1 -type f -name '*.tgz' -print -quit)"

          if [[ -z "$chart_package" ]]; then
            echo "Chart package not found in dist/" >&2
            exit 1
          fi

          helm push "$chart_package" "${{ steps.meta.outputs.chart_repo }}"
